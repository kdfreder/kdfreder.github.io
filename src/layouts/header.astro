---
import Nav from '../components/nav.astro';

export interface Props {
  currentPage?: string;
}

const { currentPage } = Astro.props;
---

<header id="header" class="bg-dark fixed w-full">
  <Nav currentPage={currentPage} />
</header>